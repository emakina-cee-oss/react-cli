import React from 'react';
import { connect } from '@cerebral/react';

const <%= name %> = () => {
    return (
        <div>
            ...
        </div>
    );
};

export default connect(
    {},
    <%= name %>
);
