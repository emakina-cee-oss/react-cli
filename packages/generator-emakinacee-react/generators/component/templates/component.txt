import React from 'react';
import styles from './<%= name %>.module.scss';

/**
 * <%= nameUpperCase %>
 *
 * @returns {XML} -
 */
const <%= name %> = () => {
    return (
        <div className={styles.wrapper}>
            ...
        </div>
    );
};

export default <%= name %>;
