import React from 'react';
import styles from './<%= name %>.scss';

/**
 * <%= nameUpperCase %>
 *
 * @returns {XML} -
 */
const <%= name %> = () => {
    return (
        <div className={styles.<%= nameDash %>}>
            ...
        </div>
    );
};

export default <%= name %>;
